# Core agents

The root operator stays responsible for final integration.

Sub-agents are lightweight role definitions, not hidden automation. They exist
to keep delegation explicit and auditable inside the product repo.
