# Agent team

This repo keeps its team structure in markdown so the workflow stays visible in
Git.

Use these entrypoints when splitting work:

- `.agents/core/teams/product-bootstrap.team.md` for the overall team shape
- `.agents/core/agents/sub-agents/` for role-specific delegation
- `.agents/skills/` for durable process skills
