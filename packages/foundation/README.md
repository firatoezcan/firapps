# @firapps/foundation

Shared metadata and copy for `firapps` applications.

This package is intentionally small.

It exists so the initial web app consumes real workspace code from day one
instead of living as an isolated starter.

Current exports cover:

- repo identity and doctrine copy
- command-surface cards
- repo boundary cards
- reviewer claim cards
- product-bootstrap team cards
